#pragma once
// Embedded HTML documents. The interface ships inside the executable, so the
// app stays a single file with no web assets to copy around.
#define IDR_APP_HTML  101
#define IDR_LOCK_HTML 102
#define IDR_TRAY_HTML 103

#define IDR_MEDIA_HTML 104
// Numerically lowest icon ID, so Explorer and the taskbar use it as the app icon.
#define IDI_APPICON    1
